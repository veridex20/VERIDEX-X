[project]
name = "veridex-x"
version = "0.1.0"
description = "VERIDEX X - Nature Intelligence Operating System"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
